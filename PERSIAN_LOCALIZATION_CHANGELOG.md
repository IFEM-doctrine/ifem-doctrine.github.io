# Persian Localization Rewrite — 2026-08-15

Rewritten Persian content across all current `/fa/` pages:

- `/fa/`
- `/fa/doctrine/`
- `/fa/how-it-works/`
- `/fa/ai-engineering/`
- `/fa/examples/`
- `/fa/publications/`
- `/fa/about/`

## Scope

- Replaced literal English-to-Persian phrasing with native technical Persian.
- Standardized the formal Persian rendering of IFEM as `روش‌شناسی اجرای رابط‌محور`.
- Preserved the technical distinction between verification (`راستی‌آزمایی`) and validation (`اعتبارسنجی`).
- Corrected awkward or misleading expressions such as `API اتفاقی`, `مرز یک شیء طراحی است`, `قرارداد محدود می‌کند`, `راستی‌آزمایی تصمیم می‌گیرد`, and `اجرای محدودشده`.
- Corrected the publications page so it no longer describes IFEM v2.2 as “peer-reviewed” in Persian when the English source does not make that claim.
- Updated Persian metadata and animation phase messages where the same literal phrasing appeared.
- Preserved HTML structure, URLs, canonical links, `hreflang`, DOI values, RTL settings, CSS classes, JavaScript hooks, and English pages.
